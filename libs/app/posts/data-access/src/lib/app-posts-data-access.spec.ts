import { appPostsDataAccess } from './app-posts-data-access';

describe('appPostsDataAccess', () => {
  it('should work', () => {
    expect(appPostsDataAccess()).toEqual('app-posts-data-access');
  });
});
