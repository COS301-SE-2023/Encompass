import { appCreateEventUtil } from './app-calendarPopover-util';

describe('appCreateEventUtil', () => {
  it('should work', () => {
    expect(appCreateEventUtil()).toEqual('app-create-event-util');
  });
});
