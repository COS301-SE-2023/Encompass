import { appCommentsDataAccess } from './app-comments-data-access';

describe('appCommentsDataAccess', () => {
  it('should work', () => {
    expect(appCommentsDataAccess()).toEqual('app-comments-data-access');
  });
});
