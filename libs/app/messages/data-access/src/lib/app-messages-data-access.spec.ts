import { appMessagesDataAccess } from './app-messages-data-access';

describe('appMessagesDataAccess', () => {
  it('should work', () => {
    expect(appMessagesDataAccess()).toEqual('app-messages-data-access');
  });
});
