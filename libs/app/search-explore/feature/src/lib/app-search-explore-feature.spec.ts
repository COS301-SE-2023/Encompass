import { appSearchExploreFeature } from './app-search-explore-feature';

describe('appSearchExploreFeature', () => {
  it('should work', () => {
    expect(appSearchExploreFeature()).toEqual('app-search-explore-feature');
  });
});
