import { appSearchExploreUtil } from './app-search-explore-util';

describe('appSearchExploreUtil', () => {
  it('should work', () => {
    expect(appSearchExploreUtil()).toEqual('app-search-explore-util');
  });
});
