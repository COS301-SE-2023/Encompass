import { appHomePageFeature } from './app-home-page-feature';

describe('appHomePageFeature', () => {
  it('should work', () => {
    expect(appHomePageFeature()).toEqual('app-home-page-feature');
  });
});
