import { appPrivacyFeature } from './app-privacy-feature';

describe('appPrivacyFeature', () => {
  it('should work', () => {
    expect(appPrivacyFeature()).toEqual('app-privacy-feature');
  });
});
