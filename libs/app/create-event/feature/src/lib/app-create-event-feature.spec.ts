import { appCreateEventFeature } from './app-create-event-feature';

describe('appCreateEventFeature', () => {
  it('should work', () => {
    expect(appCreateEventFeature()).toEqual('app-create-event-feature');
  });
});
