import { appSignUpDataAccess } from './app-sign-up-data-access';

describe('appSignUpDataAccess', () => {
  it('should work', () => {
    expect(appSignUpDataAccess()).toEqual('app-sign-up-data-access');
  });
});
