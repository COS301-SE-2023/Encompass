import { appHelpPageFeature } from './app-help-page-feature';

describe('appHelpPageFeature', () => {
  it('should work', () => {
    expect(appHelpPageFeature()).toEqual('app-help-page-feature');
  });
});
