import { appCreatePostDataAccess } from './app-create-post-data-access';

describe('appCreatePostDataAccess', () => {
  it('should work', () => {
    expect(appCreatePostDataAccess()).toEqual('app-create-post-data-access');
  });
});
