import { appCreatePostFeature } from './app-create-post-feature';

describe('appCreatePostFeature', () => {
  it('should work', () => {
    expect(appCreatePostFeature()).toEqual('app-create-post-feature');
  });
});
