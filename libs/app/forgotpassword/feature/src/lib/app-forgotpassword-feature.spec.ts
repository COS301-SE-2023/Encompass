import { appForgotpasswordFeature } from './app-forgotpassword-feature';

describe('appForgotpasswordFeature', () => {
  it('should work', () => {
    expect(appForgotpasswordFeature()).toEqual('app-forgotpassword-feature');
  });
});
