import { appLeaderboardFeature } from './app-leaderboard-feature';

describe('appLeaderboardFeature', () => {
  it('should work', () => {
    expect(appLeaderboardFeature()).toEqual('app-leaderboard-feature');
  });
});
