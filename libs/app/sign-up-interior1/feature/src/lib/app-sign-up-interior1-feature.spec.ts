import { appSignUpInterior1Feature } from './app-sign-up-interior1-feature';

describe('appSignUpInterior1Feature', () => {
  it('should work', () => {
    expect(appSignUpInterior1Feature()).toEqual(
      'app-sign-up-interior1-feature'
    );
  });
});
