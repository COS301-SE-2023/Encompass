import { appCreateCommunityUtil } from './app-create-community-util';

describe('appCreateCommunityUtil', () => {
  it('should work', () => {
    expect(appCreateCommunityUtil()).toEqual('app-create-community-util');
  });
});
