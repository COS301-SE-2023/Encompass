import { appCreateCommunityFeature } from './app-create-community-feature';

describe('appCreateCommunityFeature', () => {
  it('should work', () => {
    expect(appCreateCommunityFeature()).toEqual('app-create-community-feature');
  });
});
