import { appCreateCommunityDataAccess } from './app-create-community-data-access';

describe('appCreateCommunityDataAccess', () => {
  it('should work', () => {
    expect(appCreateCommunityDataAccess()).toEqual(
      'app-create-community-data-access'
    );
  });
});
