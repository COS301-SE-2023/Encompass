import { appSignUpInterior2DataAccess } from './app-sign-up-interior2-data-access';

describe('appSignUpInterior2DataAccess', () => {
  it('should work', () => {
    expect(appSignUpInterior2DataAccess()).toEqual(
      'app-sign-up-interior2-data-access'
    );
  });
});
