import { appSignUpInterior2Feature } from './app-sign-up-interior2-feature';

describe('appSignUpInterior2Feature', () => {
  it('should work', () => {
    expect(appSignUpInterior2Feature()).toEqual(
      'app-sign-up-interior2-feature'
    );
  });
});
