import { appThemesDataAccess } from './app-themes-data-access';

describe('appThemesDataAccess', () => {
  it('should work', () => {
    expect(appThemesDataAccess()).toEqual('app-themes-data-access');
  });
});
