import { appCommunityProfileDataAccess } from './app-community-profile-data-access';

describe('appCommunityProfileDataAccess', () => {
  it('should work', () => {
    expect(appCommunityProfileDataAccess()).toEqual(
      'app-community-profile-data-access'
    );
  });
});
