import { appCommunityProfileUtil } from './app-community-profile-util';

describe('appCommunityProfileUtil', () => {
  it('should work', () => {
    expect(appCommunityProfileUtil()).toEqual('app-community-profile-util');
  });
});
