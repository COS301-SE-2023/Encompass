import { appChallengeDescriptionDataAccess } from './app-challenge-description-data-access';

describe('appChallengeDescriptionDataAccess', () => {
  it('should work', () => {
    expect(appChallengeDescriptionDataAccess()).toEqual(
      'app-challenge-description-data-access'
    );
  });
});
