import { appChallengeDescriptionUtil } from './app-challenge-description-util';

describe('appChallengeDescriptionUtil', () => {
  it('should work', () => {
    expect(appChallengeDescriptionUtil()).toEqual(
      'app-challenge-description-util'
    );
  });
});
