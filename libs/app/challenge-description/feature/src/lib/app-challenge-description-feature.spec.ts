import { appChallengeDescriptionFeature } from './app-challenge-description-feature';

describe('appChallengeDescriptionFeature', () => {
  it('should work', () => {
    expect(appChallengeDescriptionFeature()).toEqual(
      'app-challenge-description-feature'
    );
  });
});
