import { appUserProfileUtil } from './app-user-profile-util';

describe('appUserProfileUtil', () => {
  it('should work', () => {
    expect(appUserProfileUtil()).toEqual('app-user-profile-util');
  });
});
