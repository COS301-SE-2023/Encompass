import { appUserProfileDataAccess } from './app-user-profile-data-access';

describe('appUserProfileDataAccess', () => {
  it('should work', () => {
    expect(appUserProfileDataAccess()).toEqual('app-user-profile-data-access');
  });
});
