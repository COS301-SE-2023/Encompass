import { appUserProfileFeature } from './app-user-profile-feature';


describe('appUserProfileFeature', () => {
  it('should work', () => {
    expect(appUserProfileFeature()).toEqual('app-user-profile-feature');
  });
});
