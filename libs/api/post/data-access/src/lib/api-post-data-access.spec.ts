import { apiPostDataAccess } from './api-post-data-access';

describe('apiPostDataAccess', () => {
  it('should work', () => {
    expect(apiPostDataAccess()).toEqual('api-post-data-access');
  });
});
