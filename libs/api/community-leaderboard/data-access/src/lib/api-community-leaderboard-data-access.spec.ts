import { apiCommunityLeaderboardDataAccess } from './api-community-leaderboard-data-access';

describe('apiCommunityLeaderboardDataAccess', () => {
  it('should work', () => {
    expect(apiCommunityLeaderboardDataAccess()).toEqual(
      'api-community-leaderboard-data-access'
    );
  });
});
