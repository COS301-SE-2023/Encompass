import { apiChatListDataAccess } from './api-chat-list-data-access';

describe('apiChatListDataAccess', () => {
  it('should work', () => {
    expect(apiChatListDataAccess()).toEqual('api-chat-list-data-access');
  });
});
