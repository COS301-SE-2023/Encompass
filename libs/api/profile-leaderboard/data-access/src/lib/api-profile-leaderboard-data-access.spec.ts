import { apiProfileLeaderboardDataAccess } from './api-profile-leaderboard-data-access';

describe('apiProfileLeaderboardDataAccess', () => {
  it('should work', () => {
    expect(apiProfileLeaderboardDataAccess()).toEqual(
      'api-profile-leaderboard-data-access'
    );
  });
});
