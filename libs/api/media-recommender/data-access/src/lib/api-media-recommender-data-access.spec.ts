import { apiMediaRecommenderDataAccess } from './api-media-recommender-data-access';

describe('apiMediaRecommenderDataAccess', () => {
  it('should work', () => {
    expect(apiMediaRecommenderDataAccess()).toEqual(
      'api-media-recommender-data-access'
    );
  });
});
