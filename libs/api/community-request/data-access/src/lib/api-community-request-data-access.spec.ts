import { apiCommunityRequestDataAccess } from './api-community-request-data-access';

describe('apiCommunityRequestDataAccess', () => {
  it('should work', () => {
    expect(apiCommunityRequestDataAccess()).toEqual(
      'api-community-request-data-access'
    );
  });
});
