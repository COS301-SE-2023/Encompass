import { apiUserEventsDataAccess } from './api-user-events-data-access';

describe('apiUserEventsDataAccess', () => {
  it('should work', () => {
    expect(apiUserEventsDataAccess()).toEqual('api-user-events-data-access');
  });
});
