import { apiSettingsDataAccess } from './api-settings-data-access';

describe('apiSettingsDataAccess', () => {
  it('should work', () => {
    expect(apiSettingsDataAccess()).toEqual('api-settings-data-access');
  });
});
