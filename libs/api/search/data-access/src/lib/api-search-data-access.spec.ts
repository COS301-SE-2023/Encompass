import { apiSearchDataAccess } from './api-search-data-access';

describe('apiSearchDataAccess', () => {
  it('should work', () => {
    expect(apiSearchDataAccess()).toEqual('api-search-data-access');
  });
});
