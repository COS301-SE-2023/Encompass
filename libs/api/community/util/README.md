# api-community-util

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build api-community-util` to build the library.

## Running unit tests

Run `nx test api-community-util` to execute the unit tests via [Jest](https://jestjs.io).
