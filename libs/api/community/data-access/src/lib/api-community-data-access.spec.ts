import { apiCommunityDataAccess } from './api-community-data-access';

describe('apiCommunityDataAccess', () => {
  it('should work', () => {
    expect(apiCommunityDataAccess()).toEqual('api-community-data-access');
  });
});
