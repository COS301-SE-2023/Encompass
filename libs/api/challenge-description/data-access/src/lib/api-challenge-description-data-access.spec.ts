import { apiChallengeDescriptionDataAccess } from './api-challenge-description-data-access';

describe('apiChallengeDescriptionDataAccess', () => {
  it('should work', () => {
    expect(apiChallengeDescriptionDataAccess()).toEqual(
      'api-challenge-description-data-access'
    );
  });
});
