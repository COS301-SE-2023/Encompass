import { apiChallengeDescriptionFeature } from './api-challenge-description-feature';

describe('apiChallengeDescriptionFeature', () => {
  it('should work', () => {
    expect(apiChallengeDescriptionFeature()).toEqual(
      'api-challenge-description-feature'
    );
  });
});
