import { apiChallengeDescriptionUtil } from './api-challenge-description-util';

describe('apiChallengeDescriptionUtil', () => {
  it('should work', () => {
    expect(apiChallengeDescriptionUtil()).toEqual(
      'api-challenge-description-util'
    );
  });
});
